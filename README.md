# cycleIQ Protocol

Shared protocol definitions for the cycleIQ ESC firmware and display firmware.

The protocol is header-only C for now so embedded firmware can include it
without adding runtime dependencies.
